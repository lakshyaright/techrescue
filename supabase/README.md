# TechRescue Supabase test schema

Apply `migrations/202607260001_create_techrescue_schema.sql` only in the `techrescue-test` project's Supabase **SQL Editor**. It creates identity profiles, engineer data, tickets, chat, attachments, notifications, rating, audit history, indexes, and Row-Level Security.
